import { Center, Loader } from "@mantine/core";

const Loading = () => {
    return (
        <Center>
            <Loader color="blue" size="lg" />
        </Center>
    );
};

export default Loading;
